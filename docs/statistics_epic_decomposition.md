Кызылай Айлин Джихангировна 
Когорта: 34  
Группа: 1  
Эпик: Статистика
---

## Модуль 1 — Экран «Рейтинг»

### Определение моделей
- DTO-модель пользователя для сети (est: 30 минут; fact: x минут).
- UI-модель строки рейтинга (место, аватар, имя, количество NFT) (est: 20 минут; fact: x минут).
- Enum сортировки (по рейтингу / по имени), значение по умолчанию — по рейтингу (est: 20 минут; fact: x минут).

### Верстка
- Экран рейтинга: UITableView, NavigationBar, кнопка сортировки (est: 2 часа; fact: x часов).
- Ячейка пользователя рейтинга (est: 2 часа; fact: x часов).
- Индикатор загрузки (UIActivityIndicator) (est: 30 минут; fact: x минут).
- Простое отображение ошибки (alert или label согласно макету) (est: 1 час; fact: x часов).

### Внутренняя логика (MVVM)
- ViewModel: `isLoading`, `users`, `error`, методы `load()`, `sort(by:)`, `selectUser()` (est: 2 часа; fact: x часов).
- Связь ViewModel → ViewController через closures, обновление UI в main thread (est: 2 часа; fact: x часов).
- ActionSheet сортировки + сохранение выбора в UserDefaults и восстановление при запуске (est: 1 час 20 минут; fact: x часов).
- Сортировка данных и обновление таблицы через `reloadData()` (est: 40 минут; fact: x минут).
- Навигация: переход на экран «User Card» по нажатию на пользователя (est: 30 минут; fact: x минут).

### Работа с сетью
- Определение эндпоинта и проверка запроса через Postman (est: 1 час; fact: x часов).
- Сервис статистики (URLSession): GET списка пользователей (est: 2 часа; fact: x часов).
- Интеграция сервиса в ViewModel, обработка ошибок (est: 1 час 30 минут; fact: x часов).
- Использование GCD: обработка данных в background, обновление UI в main (est: 40 минут; fact: x минут).

---

## Модуль 2 — Экран «User Card» 

### Определение моделей
- DTO модели пользователя (детальная) (est: 40 минут; fact: x минут).
- UI-модель карточки пользователя (аватар, имя, описание, сайт, количество NFT) (est: 20 минут; fact: x минут).

### Верстка
- Экран User Card по макету (UIKit code) (est: 2 часа 30 минут; fact: x часов).
- Индикатор загрузки (est: 20 минут; fact: x минут).

### Внутренняя логика (MVVM)
- ViewModel: `isLoading`, `user`, `error`, метод `load(userId)` (est: 1 час 30 минут; fact: x часов).
- Связь ViewModel → ViewController (est: 1 час 30 минут; fact: x часов).
- Переход в WebView по кнопке «Перейти на сайт пользователя» (без обработки edge-cases) (est: 30 минут; fact: x минут).
- Переход на экран «Коллекция пользователя» по нажатию на блок «Коллекция NFT (N)» (est: 30 минут; fact: x минут).

### Работа с сетью
- Сервис: GET пользователь по id (est: 2 часа; fact: x часов).
- Подключение сервиса к ViewModel (est: 1 час; fact: x часов).

---

## Модуль 3 — Экран «Коллекция пользователя» 

### Определение моделей
- UI-модель NFT (imageURL, name, rating, priceETH, isLiked, isInCart) (est: 40 минут; fact: x минут).
- DTO модель NFT для сети (est: 40 минут; fact: x минут).

### Верстка
- Экран коллекции (UICollectionView) (est: 1 час 30 минут; fact: x часов).
- Ячейка NFT (est: 3 часа; fact: x часов).
- Настройка layout коллекции в соответствии с макетом (est: 1 час 30 минут; fact: x часов).
- Индикатор загрузки / loader-состояние (est: 40 минут; fact: x минут).

### Внутренняя логика (MVVM)
- ViewModel: `isLoading`, `nfts`, `error`, методы `load()`, `toggleLike(id)`, `toggleCart(id)` (est: 2 часа; fact: x часов).
- Связь ViewModel → ViewController (est: 2 часа; fact: x часов).
- Обновление коллекции через `reloadData()` (est: 20 минут; fact: x минут).
- Навигация: переход на экран NFT по нажатию на ячейку (роутинг, без реализации экрана NFT) (est: 30 минут; fact: x минут).

### Работа с сетью
- Получение списка NFT пользователя (id) и догрузка NFT по id с использованием DispatchGroup (est: 4 часа; fact: x часов).
- PUT-запрос для обновления лайков (если предусмотрено сервером проекта) (est: 2 часа; fact: x часов).
- Интеграция с корзиной проекта (использование общего cart storage/сервиса) (est: 1 час 30 минут; fact: x часов).




